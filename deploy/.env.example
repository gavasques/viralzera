# === Core ===
NODE_ENV=production
PORT=5000
APP_URL=https://www.viralzera.com.br

# Frontend URL permitida pelo CORS (mesmo que APP_URL em produção)
FRONTEND_URL=https://www.viralzera.com.br

# === Database (PostgreSQL no Docker Swarm — container postgres_postgres) ===
DATABASE_URL=postgresql://viralzera_user:CHANGE_ME@postgres_postgres:5432/viralzera

# === Autenticação ===
JWT_SECRET=CHANGE_ME_RANDOM_64_CHARS_MIN

# === APIs de IA ===
OPENROUTER_API_KEY=CHANGE_ME
OPENAI_API_KEY=CHANGE_ME

# === Adicione aqui outras variáveis necessárias ===
# Ex: chaves de stripe, serviços de email, storage, etc.
